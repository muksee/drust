[package]
name = "lazy-static-example"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lazy-static = { path = "../lazy-static"}
regex = "1.6"